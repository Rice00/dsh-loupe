# dsh-loupe · 实现方案（待评审）

> 状态：**讨论稿，尚未写码**。评审通过后按 §9 的里程碑推进。
> 名字：`dsh-loupe`（loupe = 钟表匠的寸镜，看细节用）。npm 未占用，社区列表无同名。

---

## 0. 一句话

选中文字 → 选区下方浮出「解释 / 设置」→ 解释在**独立浮窗**里完成（不碰主会话、不建会话）→ 想深入时把这段对话**固定成一个正式会话**，进左侧列表继续问。

---

## 1. 目标与优先级

按此顺序排优先级，出现取舍时上位优先：

1. **快** —— 首字延迟尽量低
2. **准** —— 解释质量（局部上下文 + 插件自有上下文）
3. **轻量** —— 不污染主会话、不在会话列表留垃圾
4. **省 token** —— 每次请求带的上下文必须小

**明确的非目标**：

- 不做侧边栏（不占右列、不注册右栏 tab、不与 better-sidebar 竞争）
- 不导入 agent 全量上下文（会话史 / 系统提示 / 工具调用记录）
- 不修改会话列表里已存在的会话

---

## 2. 已定决策（共识快照）

| 项 | 定稿 | 依据 |
| --- | --- | --- |
| 名字 | `dsh-loupe` | npm 未占；避免 `dsh-explain`（社区已有 yuezengwu/dsh-explain）、`dsh-margin`/`dsh-annotate`（npm 已占） |
| 入口 | 选中即弹，浮标落在**选区下方** | 用户定 |
| 浮标项 | a. 解释　b. 设置图标 | 用户定 |
| 通道 | **独立**：host 侧 `llm.stream`，不建会话、不写会话日志、不注册工具 | 用户定；也是"不污染"的技术前提 |
| 上下文档位 | 邻域 **0 条**（设置可调 0–4）；注入插件自有上下文；**不塞** agent 上下文 | 用户定 |
| 插件上下文 | `~/.dsh/loupe/context.md`（用户可编辑）+ 设置页可改 + **自动累积默认开** | 用户定 |
| 浮窗实例 | **单实例**（新开替换旧的） | 用户定 |
| 固定落点 | **新建空白会话**（`sessions.create`），不是 fork | 用户定；token 论证见 §6 |
| 历史记录 | **host 侧磁盘文件**，无条数上限、不区分工作区、全部留存 | 用户定 |
| 设置面板 | 参数 + 完整历史列表 + 复原某条对话回浮窗 | 用户定 |

---

## 3. 架构

### 3.1 两半结构（DSH 插件规范：host + client）

**client 半端**（跑在浏览器页面里）

- 选区探测：限定在消息列表容器内（`[data-chat-flow]`），排除输入框、输入区、contenteditable，以及自家浮窗与浮标
- 浮标：两个按钮，锚定选区矩形下方；Escape / 滚动 / 点击别处收起
- 浮窗：单实例、可拖拽、流式 Markdown 渲染、输入框（预填「所选内容 + 提问句」）、可多轮
- 设置卡片：走 settings 槽位注册
- 历史列表：设置卡片内 + 浮窗内一个入口

**host 半端**（跑在 DSH 的 node 进程里）

- `POST /plugins/dsh-loupe/explain`（SSE 流式）：读局部上下文 → 拼 prompt → `llm.stream` → 逐块回传
- `GET /plugins/dsh-loupe/history`、`DELETE`：历史读写
- `POST /plugins/dsh-loupe/pin`：把浮窗对话固定成正式会话
- 注册设置命名空间（让设置卡片出现在 设置 → 插件）
- 同源信任围栏（`Origin` host 必须等于请求 `Host`；防 DNS rebinding / 跨站）

### 3.2 一次解释的调用链

```
用户划选 → client 探测选区 + 定位所在消息
        → 浮标（解释 / 设置）
        → 点「解释」→ 浮窗打开，输入框预填「请解释下面这段内容：<选区>」
        → 用户回车
        → client POST /plugins/dsh-loupe/explain（SSE）
        → host：组装 prompt（§4）→ llm.stream
        → 逐块回传 → 浮窗流式渲染
        → 问答成对写入 history.jsonl
```

关键：**全程没有会话实体**。没有会话，就没有归档、没有会话列表垃圾，也不需要 unarchive API。

---

## 4. 上下文策略（本插件的核心）

### 4.1 分层

| 层 | 内容 | 是否注入 | 预算 |
| --- | --- | --- | --- |
| 系统提示 | 固定短小的一段：你是解释助手，只解释不改写 | 必注入 | ~100 token |
| 插件上下文 | `context.md` 全文（术语表 / 风格指令 / 累积结论） | 可注入、可开关 | **≤1500 token** |
| 所在消息 | 选区那条消息的**全文** | 必注入 | ≤2000 字符 |
| 邻域 | 前后各 N 条 | **默认关** | 0–4 条 |
| 选区 | 选中文字本身 | 必注入 | ≤2000 字符 |
| 用户问题 | 输入框内容 | 必注入 | ≤2000 字符 |
| agent 上下文 | 会话史 / 系统提示 / 工具记录 | **不注入** | — |

### 4.2 顺序（为 prompt 前缀缓存服务）

```
[稳定] 系统提示 → 插件上下文          ← 每次请求都一样，利于命中缓存
[变化] 所在消息 → 邻域 → 选区 → 问题  ← 每次不同
```

本机会话面板显示「缓存命中 99.7%」，说明宿主按前缀命中缓存。把稳定段放最前、且内容尽量不变，这部分几乎不额外耗时。

### 4.3 措辞纪律

- 插件上下文写成**参考**，不写「必须遵守」「严格按此术语」——硬指令会让模型硬套术语，反而降质量
- 超预算时的处理：按行截断并标记 `（插件上下文已截断）`，不静默丢弃

### 4.4 透明性

浮窗底部常驻一行统计：`注入 选区 N 字 · 所在消息 M 字 · 插件上下文 K 字`。
解释变差时能立刻判断是不是上下文的锅——没有这行，质量问题是不可诊断的。

---

## 5. 数据落地

两个文件，都在 `~/.dsh/loupe/`：

**`context.md`** —— 用户可编辑的常驻上下文（跟 AGENTS.md 一个手感）

- 手动区：术语表、风格指令（用户自己写，插件不覆盖）
- 自动区：累积的既往解释结论（插件追加，带标记，受 token 预算约束）
- 设置页里可直接编辑（host 侧读写 + client 侧 textarea）

**`history.jsonl`** —— 追加式历史，一行一条，无上限

```json
{"id":"...","at":1790056738293,"sessionId":"session-...","messageId":"...","selection":"...","question":"...","answer":"...","model":"...","pinned":false}
```

选 jsonl 而不是单个 json 数组的理由：追加写不用重写全文件、大历史不怕、损坏只丢一行、可 tail 排查。

---

## 6. 为什么「固定」用新建空白会话而不是 fork

四个目标都指向新建：

| 方案 | 首轮 prompt 带什么 | 量级 |
| --- | --- | --- |
| fork 主会话 | 系统提示 + **主会话全量历史** + 浮窗问答 + 新问题 | 几十万 token（本会话 51 轮 463 步，日志就在这个量级） |
| **新建空白会话** | 系统提示 + **只有浮窗问答** + 新问题 | 几百到一两千 token |

差 2–3 个数量级。且 fork 的首轮必须全量 prefill（新会话第一次请求，缓存必然 miss），首字延迟明显变长。

**已核实 API 存在**（`@deepseek-ai/dsh-api-session-controller`）：

- `sessions.create(opts)` → `remote.session.create`，新建空白会话（源码注释：*A created session is blank by definition*）
- `sessions.fork(opts)` → `remote.session.fork`，返回子会话 id
- 两者均有结构化错误类型（`SessionCreateError` / `SessionForkError`）

**关键澄清**：浮窗对话本身**不是会话也不需要归档**——归档是 sidenote 侧边对话（真 fork）的行为。DSH 的归档会话刻意不可打开、Host 也没有 unarchive API，所以「固定」只能**正向创建**，不能反向捞归档。这恰好是本方案比 sidenote 干净的地方。

---

## 7. 动手前必须验证的三个技术点

先写一次性探针（每个 30 行以内），验证通过再进入实现：

| # | 待验证 | 不通过的退路 |
| --- | --- | --- |
| P1 | client 插件能否调用 `sessions.create`；新建的空白会话如何**写入历史而不触发一次模型回答** | 退路 A：把最后一段问答作为首条消息发进去（多一次回答）；退路 B：固定为「导出 markdown 到工作区」而非会话 |
| P2 | host 侧 `llm.stream` 的可用性、流式回传、模型路由（用主会话当前模型） | 回退到 client 侧 `session.prompt` 走 /btw 式侧问 |
| P3 | 从选区 DOM 反查「所在消息」的全文与 sessionId | 退路：只注入选区 + 用户手贴上下文 |

---

## 8. 预计文件清单

```
dsh-loupe/
├── package.json              # dsh.bundle.patch + client 平台声明
├── cordis.patch.yml          # - insert: - id: loupe / name: dsh-loupe
├── README.md
├── PLAN.md                   # 本文件
├── src/
│   ├── index.ts              # host：SSE 路由 / 历史读写 / pin / 设置命名空间
│   ├── trust-fence.ts        # 同源围栏
│   ├── context.ts            # context.md 读写 + 累积 + 预算截断
│   ├── history.ts            # history.jsonl 追加 / 读取
│   └── client/
│       ├── index.tsx         # 注册槽位与浮标
│       ├── selection.ts      # 选区探测 + 消息定位
│       ├── popover.tsx       # 浮标（解释 / 设置）
│       ├── popup.tsx         # 浮窗（流式 + 多轮 + 拖拽）
│       ├── settings.tsx      # 设置卡片
│       └── history.tsx       # 历史列表 + 复原
└── lib/                      # 构建产物（host ESM + client bundle）
```

---

## 9. 里程碑与验证方式

| 里程碑 | 内容 | 验证 |
| --- | --- | --- |
| **M0 探针** | 三个探针（§7） | 探针脚本跑通，输出证据贴回 |
| **M1 最小可跑** | 选中 → 浮标 → 浮窗 → 一次解释返回 | 真机点一下，看到流式回答；console 无错 |
| **M2 上下文** | `context.md` 读写 + 预算截断 + 稳定段前置 + 统计行 | 改 context.md 后解释口径随之变化；统计行数字与实际注入一致 |
| **M3 历史** | `history.jsonl` 全量留存 + 设置卡片里能翻 | 关掉浮窗后能在设置里找到刚才那条 |
| **M4 固定** | 从浮窗/历史固定成正式会话 | 左侧列表出现新会话，点进去接着问 |
| **M5 设置卡片** | 参数项齐全（邻域条数、上下文开关与预算、累积开关、模型） | 逐项改完即时生效 |

每个里程碑都**真机挂载**验证（不是只看编译通过）。

---

## 10. 已知风险

| 风险 | 说明 | 应对 |
| --- | --- | --- |
| 宿主槽位契约变动 | DSH 迭代快，槽位/服务名可能改 | 能力探测（缺能力时降级而非崩），像 sidenote 的 probeHost 那样把降级摆到开发者工具里 |
| SSE 路由信任域 | 与 DSH 本体同源、localhost | 同源围栏 + 只读会话日志，不暴露完整日志 |
| `llm.stream` 是否受主会话排队影响 | 主任务跑长回合时侧问是否被卡 | M0 探针里一并测；若被卡则改走独立 provider 调用 |
| 长选区 / 超长消息 | 用户可能选一整段长文 | 各处按预算截断并显式标记 |
| 自动累积失控 | 累积结论越攒越长，反拖慢速度 | 预算硬上限 + 超限时按最近优先保留 + 设置里可一键清空自动区 |

---

## 11. 待用户确认

1. 本方案整体是否通过（特别是 §6 的「新建空白会话」定稿）
2. 是否先执行 **M0 三个探针**再进入实现
